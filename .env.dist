# Drupal Backend URL
# For development with mock data, use the local Next.js API server that hosts mock data:
NEXT_PUBLIC_DRUPAL_BASE_URL="http://localhost:3000/api"
# For production, point to your real Drupal backend:
# NEXT_PUBLIC_DRUPAL_BASE_URL="https://your-drupal-backend.example.com"

# Development: Allow self-signed SSL certificates (DDEV, GitHub Codespaces, etc.)
# WARNING: Only use in development! Never in production!
NODE_TLS_REJECT_UNAUTHORIZED="0"
